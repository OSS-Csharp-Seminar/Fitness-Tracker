@inherits LayoutComponentBase
@using Microsoft.AspNetCore.Components.Web
@using Microsoft.AspNetCore.WebUtilities
@inject NavigationManager NavigationManager

<PageTitle>PushApps - Fitness aplikacija</PageTitle>
<HeadContent>
    <link rel="stylesheet" href="/userlogin1.css" />
</HeadContent>

<div class="hero-page">
    <div class="nav-container">
        <nav class="navbar">
            <div class="navbar-brand">
                <a href="@GetUrlWithParams("/")">PushApps</a>
            </div>
            <div class="navbar-links">
                <a href="@GetUrlWithParams("/workouts/daily-training")" class="nav-link @GetMainActiveClass("workouts")">WORKOUTS</a>
                <a href="@GetUrlWithParams("/my-plan/dashboard")" class="nav-link @GetMainActiveClass("my-plan")">MY PLAN</a>
                <a href="@GetUrlWithParams("/meals/today")" class="nav-link @GetMainActiveClass("meals")">MEALS</a>
                <a href="@GetUrlWithParams("/profile/personal")" class="nav-link @GetMainActiveClass("profile")">MY PROFILE</a>
            </div>
        </nav>
    </div>

    <div class="content-wrapper">
        <div class="sidebar">
            <h4 class="sidebar-title">@GetSidebarTitle()</h4>
            <ul class="sidebar-menu">
                @if (CurrentSection == "workouts")
                {
                    <li class="sidebar-item">
                        <a href="@GetUrlWithParams("/workouts/daily-training")" class="sidebar-link @GetActiveClass("/workouts/daily-training")">
                            Day Training
                        </a>
                    </li>
                    <li class="sidebar-item">
                        <a href="@GetUrlWithParams("/workouts/training-history")" class="sidebar-link @GetActiveClass("/workouts/training-history")">
                            Training History
                        </a>
                    </li>
                    <li class="sidebar-item">
                        <a href="@GetUrlWithParams("/workouts/view-all")" class="sidebar-link @GetActiveClass("/workouts/view-all")">
                            View all Workouts
                        </a>
                    </li>
                }
                else if (CurrentSection == "my-plan")
                {
                    <li class="sidebar-item">
                        <a href="@GetUrlWithParams("/my-plan/dashboard")" class="sidebar-link @GetActiveClass("/my-plan/dashboard")">
                            Dashboard
                        </a>
                    </li>
                    <li class="sidebar-item">
                        <a href="@GetUrlWithParams("/my-plan/create-plan")" class="sidebar-link @GetActiveClass("/my-plan/create-plan")">
                            Create New Plan
                        </a>
                    </li>
                    <li class="sidebar-item">
                        <a href="@GetUrlWithParams("/my-plan/my-plans")" class="sidebar-link @GetActiveClass("/my-plan/my-plans")">
                            My Plans
                        </a>
                    </li>
                    <li class="sidebar-item">
                        <a href="@GetUrlWithParams("/my-plan/progress")" class="sidebar-link @GetActiveClass("/my-plan/progress")">
                            Progress Tracking
                        </a>
                    </li>
                    <li class="sidebar-item">
                        <a href="@GetUrlWithParams("/my-plan/statistics")" class="sidebar-link @GetActiveClass("/my-plan/statistics")">
                            Statistics
                        </a>
                    </li>
                }
                else if (CurrentSection == "meals")
                {
                    <li class="sidebar-item">
                        <a href="@GetUrlWithParams("/meals/today")" class="sidebar-link @GetActiveClass("/meals/today")">
                            Today's Meals
                        </a>
                    </li>
                    <li class="sidebar-item">
                        <a href="@GetUrlWithParams("/meals/add")" class="sidebar-link @GetActiveClass("/meals/add")">
                            Add Meal
                        </a>
                    </li>
                    <li class="sidebar-item">
                        <a href="@GetUrlWithParams("/meals/history")" class="sidebar-link @GetActiveClass("/meals/history")">
                            Meal History
                        </a>
                    </li>
                    <li class="sidebar-item">
                        <a href="@GetUrlWithParams("/meals/recipes")" class="sidebar-link @GetActiveClass("/meals/recipes")">
                            Recipes
                        </a>
                    </li>
                    <li class="sidebar-item">
                        <a href="@GetUrlWithParams("/meals/nutrition")" class="sidebar-link @GetActiveClass("/meals/nutrition")">
                            Nutrition Tracker
                        </a>
                    </li>
                    <li class="sidebar-item">
                        <a href="@GetUrlWithParams("/meals/shopping")" class="sidebar-link @GetActiveClass("/meals/shopping")">
                            Shopping List
                        </a>
                    </li>
                }
                else if (CurrentSection == "profile")
                {
                    <li class="sidebar-item">
                        <a href="@GetUrlWithParams("/profile/personal")" class="sidebar-link @GetActiveClass("/profile/personal")">
                            Personal Info
                        </a>
                    </li>
                    <li class="sidebar-item">
                        <a href="@GetUrlWithParams("/profile/physical")" class="sidebar-link @GetActiveClass("/profile/physical")">
                            Physical Data
                        </a>
                    </li>
                    <li class="sidebar-item">
                        <a href="@GetUrlWithParams("/profile/preferences")" class="sidebar-link @GetActiveClass("/profile/preferences")">
                            Preferences
                        </a>
                    </li>
                    <li class="sidebar-item">
                        <a href="@GetUrlWithParams("/profile/account")" class="sidebar-link @GetActiveClass("/profile/account")">
                            Account Settings
                        </a>
                    </li>
                    <li class="sidebar-item">
                        <a href="@GetUrlWithParams("/profile/notifications")" class="sidebar-link @GetActiveClass("/profile/notifications")">
                            Notifications
                        </a>
                    </li>
                }
            </ul>
        </div>

        <div class="main-content">
            @Body
        </div>
    </div>
</div>

<footer class="nav-container">
    @if (!string.IsNullOrEmpty(userName))
    {
        <div class="text-muted">
            Logged in as: @userName (ID: @userId)
        </div>
    }
</footer>

@code {
    private string userId = "";
    private string userName = "";

    protected override void OnInitialized()
    {
        ExtractUserParams();
    }

    protected override void OnParametersSet()
    {
        ExtractUserParams();
    }

    private void ExtractUserParams()
    {
        // Dohvati parametre iz URL-a (oba načina)
        var uri = new Uri(NavigationManager.Uri);
        var query = QueryHelpers.ParseQuery(uri.Query);

        if (query.TryGetValue("userId", out var userIdValue))
            userId = userIdValue;

        if (query.TryGetValue("userName", out var userNameValue))
            userName = userNameValue;

        // Ako nema query parametara, provjeri route parametre
        if (string.IsNullOrEmpty(userId) || string.IsNullOrEmpty(userName))
        {
            var segments = uri.AbsolutePath.Split('/', StringSplitOptions.RemoveEmptyEntries);
            if (segments.Length >= 2)
            {
                var lastSegment = segments[segments.Length - 1];
                var secondLastSegment = segments[segments.Length - 2];

                // Provjeri da li nisu nazivi stranica
                if (!IsPageName(lastSegment) && !IsPageName(secondLastSegment))
                {
                    userName = lastSegment;
                    userId = secondLastSegment;
                }
            }
        }
    }

    private bool IsPageName(string segment)
    {
        var pageNames = new[] {
            "daily-training", "training-history", "view-all",
            "dashboard", "create-plan", "my-plans", "progress", "statistics",
            "today", "add", "history", "recipes", "nutrition", "shopping",
            "personal", "physical", "preferences", "account", "notifications"
        };
        return pageNames.Contains(segment);
    }

    private string CurrentSection
    {
        get
        {
            var currentPath = NavigationManager.ToBaseRelativePath(NavigationManager.Uri);

            if (currentPath.StartsWith("workouts"))
                return "workouts";
            else if (currentPath.StartsWith("my-plan"))
                return "my-plan";
            else if (currentPath.StartsWith("meals"))
                return "meals";
            else if (currentPath.StartsWith("profile"))
                return "profile";

            return "workouts";
        }
    }

    private string GetSidebarTitle()
    {
        return CurrentSection switch
        {
            "workouts" => "Workouts",
            "my-plan" => "My Plan",
            "meals" => "Meals",
            "profile" => "Profile",
            _ => "Navigation"
        };
    }

    private string GetActiveClass(string path)
    {
        var currentPath = NavigationManager.ToBaseRelativePath(NavigationManager.Uri);
        return currentPath.StartsWith(path.TrimStart('/')) ? "active" : "";
    }

    private string GetMainActiveClass(string section)
    {
        return CurrentSection == section ? "active" : "";
    }

    /// KLJUČNA METODA - uvijek dodaje userId i userName na sve linkove
    private string GetUrlWithParams(string baseUrl)
    {
        if (!string.IsNullOrEmpty(userId) && !string.IsNullOrEmpty(userName))
        {
            return $"{baseUrl}?userId={userId}&userName={userName}";
        }
        return baseUrl;
    }

    [Parameter]
    public RenderFragment LeftContent { get; set; }

    [Parameter]
    public RenderFragment RightContent { get; set; }
}